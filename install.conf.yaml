- defaults:
    link:
      relink: true

- clean: ['~']

- link:
  ~/.tmux.conf: tmux.conf
  ~/.tmux: tmux
  ~/.vim: vim
  ~/.vimrc: vimrc
  ~/.zshrc: zshrc

- create:
  - ~/Work

#- shell:
  #  - [git submodule update --init --recursive, Installing submodules]
